# --- Robust den Repo-Root bestimmen (Ordner der .envrc) ---
ROOT="$(cd "$(dirname "$DIRENV_FILE")" && pwd -P)"

# --- lokales venv unter <repo>/.venv ---
VENV_DIR="${ROOT}/.venv"
if [ ! -d "$VENV_DIR" ]; then
  python3 -m venv "$VENV_DIR"
fi
# venv aktivieren (direnv rollt beim Verlassen automatisch zurück)
source "$VENV_DIR/bin/activate"


export DBT_PROFILES_DIR="$PWD/profiles"
export GCP_PROJECT="test-project-446313"
export BQ_DATASET="analytics_dev"
export BQ_LOCATION="EU"
export BQ_SA_EMAIL="dbt-connector@test-project-446313.iam.gserviceaccount.com"
